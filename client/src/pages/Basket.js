import { observer } from "mobx-react-lite";
//import { fetchBasketProducts, getBasket } from "../API/basketAPI";

const Basket = observer(() => {
    return <div>basket</div>;
});

export default Basket;
